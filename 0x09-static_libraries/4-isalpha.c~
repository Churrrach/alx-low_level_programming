#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
* _isalpha - checks if character is alphabet
* @c: value to check ascii
*
* Return: returns int
*/
int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}
return (0);
}
